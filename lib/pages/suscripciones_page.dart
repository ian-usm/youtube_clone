import 'package:flutter/material.dart';

class SuscriptionsPage extends StatelessWidget {
  const SuscriptionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('suscripciones', style: TextStyle(color: Colors.white),),);
  }
}