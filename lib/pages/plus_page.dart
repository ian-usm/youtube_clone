import 'package:flutter/material.dart';

class PlusPage extends StatelessWidget {
  const PlusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('plus', style: TextStyle(color: Colors.white),),);
  }
}